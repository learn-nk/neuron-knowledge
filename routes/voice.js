const express = require('express');
const router = express.Router();

// Placeholder route
router.post('/', async (req, res) => {
  res.json({ message: 'Voice route coming soon' });
});

module.exports = router;